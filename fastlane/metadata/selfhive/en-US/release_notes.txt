What's new in 1.7.1:

• Face ID now required — App Lock uses actual biometrics instead of allowing passcode bypass
• Server settings redesigned — organized into Connection and API Key sections with a proper Done button
• Collapsible Pay URLs — bill payment links slide open on tap, keeping the Pay section clean
• Monthly Checklist syncs with web — same items, same labels, same checked states across platforms
